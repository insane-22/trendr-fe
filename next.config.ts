import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["x3aeqcrof2.ufs.sh"], // add your backend or image host here
  },
};

export default nextConfig;
